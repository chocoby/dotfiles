# prefix を C-t に変更
unbind C-b
set-option -g prefix ^T
bind a send prefix
# scrollback
set-option -g history-limit 10000

# UTF-8 を使う
set-window-option -g utf8 on
# ウィンドウ名が自動的に更新されないようにする
set-window-option -g automatic-rename off

# ウィンドウマネージャ向けのタイトル
set-option -g set-titles on
set-option -g set-titles-string "#S @ #H"

# 256 色を使用
set-option -g default-terminal "screen-256color"

# ステータスライン
# ステータス行を更新する間隔
set-option -g status-interval 5
# 表示する長さ
set-option -g status-left-length 100
set-option -g status-right-length 100
# 左側に表示する内容
set-option -g status-left "[#S] "
# 右側に表示する内容
set-option -g status-right "[#(whoami)@#H][#(cat /proc/loadavg|cut -d' ' -f1)][%Y/%m/%d(%a)%H:%M]"
# ステータス行を白背景に黒文字にする
set-option -g status-bg white
set-option -g status-fg black
# ステータス行のアクティブウィンドウの表示は黒背景に白文字にする
set-window-option -g window-status-current-bg black
set-window-option -g window-status-current-fg white
set-window-option -g window-status-current-attr bold

bind r source-file ~/.tmux.conf

bind : command-prompt

bind c new-window -c "#{pane_current_path}"
bind s split-window -vc "#{pane_current_path}"
bind v split-window -hc "#{pane_current_path}"

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind < resize-pane -L 1
bind > resize-pane -R 1
bind - resize-pane -D 1
bind + resize-pane -U 1

setw -g mode-keys vi

# 環境ごとの設定ファイルを読み込み
source-file ~/.tmux.conf.local

# For Mac OS X
#
# samples/tmux.conf.local を .tmux.conf.local としてシンボリックリンクを張る
#   ln -s ~/dotfiles/samples/tmux.conf.local ~/.tmux.conf.local
