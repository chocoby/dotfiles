#!/bin/bash

{{ if eq .chezmoi.os "linux" -}}
# chezmoi runs scripts in a non-login shell, so ~/.zprofile PATH is not applied.
{{ template "prepend-path.sh" -}}
prepend_path "$HOME/.local/bin"
export PATH
{{ end -}}

if command -v rtk &>/dev/null; then
  rtk init -g
fi
